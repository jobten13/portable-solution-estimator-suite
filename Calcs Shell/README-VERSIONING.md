# Version control (suite)

**Canonical documentation:** **[`../VERSIONING.md`](../VERSIONING.md)** at the **Calcs Final** root.

- **`version.json`** lives at **`../version.json`** (not inside `Calcs Shell`).
- Optional bump script: **`create-version.ps1`** in this folder — updates **`../version.json`**.

Do **not** duplicate the suite version number in multiple README files; edit **`version.json`** (or use the script) only.
