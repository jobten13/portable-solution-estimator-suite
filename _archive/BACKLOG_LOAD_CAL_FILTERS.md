# Backlog – Load Basic / Load Pro

## Non-zero only (view filter)

**Idea:** Add a filter similar in spirit to Consumables/Medicines **“Non-zero only”** — e.g. show only equipment rows where quantity (and/or line load) is non-zero, to reduce clutter while working the sheet.

**Scope:** Load Calc Basic and Load Calc Pro (when picked up).

**Notes:** Does not by itself fix print/collapse behaviour; print rules would need to align if “print should match filtered view” is desired later.

**Status:** Not started — captured for later.
