# OS
.DS_Store
Thumbs.db
# Editor / IDE
*.swp
*.swo
*~
.idea/
.vscode/
# Logs
*.log
# Legacy / backup folders (keep in repo if you prefer; remove these lines to track OLD/)
# OLD/

# Archived snapshots/future references
# Ignore the archive folders AND their contents.
OLD/
OLD/**
**/OLD/
**/OLD/**
OLD/*
OLD/*/*
**/OLD/*
**/OLD/*/*

# Fallback: ignore all file types inside archived OLD folders
OLD/*.md
OLD/*.js
OLD/*.pdf
OLD/*.html
OLD/*.ps1
OLD/*.zip
OLD/*.json
OLD/*.css
OLD/*.txt
OLD/*.md
**/OLD/*.md

# Debug: explicit ignore for a single file
OLD/CURSOR_INSTRUCTIONS.md

# Playwright test harness (keep test code committed; ignore generated artifacts)
tests/node_modules/
tests/reports/
tests/test-results/
tests/functional-fixtures/
tests/package-lock.json
test-results/

# Historical snapshots and planning docs (kept on disk, not tracked)
_archive/
